int factorial(int n) {
    int sum = 1;
    while (n > 1) {
        sum = sum * n;
        n = n - 1;
    }
    return sum;
}

int main() {
    int x = getint();
    int res = factorial(x);
    putint(res);
    putchar(10);
    return 0;
}
